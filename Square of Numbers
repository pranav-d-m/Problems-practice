#include <stdio.h>
#include<string.h>
#include <math.h>
#include<stdlib.h>
int main()
{
    int n;

    printf("Enter n value: ");
    scanf("%d",&n);
    
    for(int i = 0; i <= n; i++)
    {
        int sq = (i+1)*(i+1);
        printf("%d\n",sq);
    }
    return 0;
}
