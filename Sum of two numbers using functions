#include <stdio.h>
int add(int a, int b);
int main() 
{
    int num1 = 5, num2 = 9, res;

    res = add(num1,num2);
    printf("Sum = %d",res);
    
    return 0;
}
int add(int a, int b)
{
    return a + b;
}
