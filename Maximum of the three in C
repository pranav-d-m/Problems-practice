#include <stdio.h>
int main()
{
    int a,b,c;// a  b  c

    printf("Input 3 numbers:\n");
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&c);

    int max = a > b ? (b > c ? a : b) : (c > b ? c : b);
    printf("Maximum of the three is : %d", max);
}
