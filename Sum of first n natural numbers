#include <stdio.h>
#include<stdlib.h>
int main()
{
    int n,sum = 0;
    printf("Enter the number n: ");
    scanf("%d",&n);
    for(int i = 1; i <= n ; i++){
        sum += i;
    }
    printf("The sum of first n natural numbers is:%d",sum);
}
