#include <stdio.h>
#include<string.h>
#include <math.h>
#include<stdlib.h>
int main()
{
    int a,b,i,sum;

    printf("Enter a and b: ");
    scanf("%d %d",&a,&b);
    
    for(i = 0; i <= b; i++)
    {
        sum = a+i;
        if(sum % 2 == 1)
            printf("%d\n",sum);
    }
}
