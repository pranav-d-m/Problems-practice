#include <stdio.h>

int main() 
{ 
    int n, i;


    int arr[6] = {3,7,5,5,2,8};

    for(int j = 0; j < 6; j++)
    {
        for(int i = 0; i < 6; i++)
        {
            if(arr[j]+arr[i] == 10)
            {
                printf("(%d,%d)",arr[j],arr[i]);
            }
        }
    }
    return 0;
}
